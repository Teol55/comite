{% extends 'base.html.twig' %}
{% block title %}{{ title }}{% endblock %}

{% block body %}

    <div class="container">
        <div class="row justify-content-center ticket__title">
            <div class="col-md-4 "><h1>{{ title }}</h1></div>
        </div>

        <div class="row tool__table">
            <table class="table table-striped">
                <thead class="thead-dark">
                <tr>
                    <th scope="col">Articles</th>
                    <th scope="col">Tarifs/Semaine</th>
                </tr>
                </thead>
                <tbody>
            {% for tool in tools  %}

                <tr>
                    <th scope="row">{{ tool.name }}</th>
                    <td>{{ tool.price }}€</td>
                </tr>
            {% endfor %}
                </tbody>
            </table>

        </div>
    </div>

{% endblock %}